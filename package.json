{
  "name": "web-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "execute": "npm run build && node ./out/core/main",
    "execute-debug": "npm run build && node --inspect ./out/core/Main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^16.1.0"
  }
}
