import { Message } from "../../entities/Message";

const AUX = Message.auxReplace;

export class Messages {
    
    public static readonly EXCEMESS0001 = `Cannot read configuration file, check log for more information`;
    public static readonly EXCEMESS0002 = `Cannot generate a generic file name.`;

}